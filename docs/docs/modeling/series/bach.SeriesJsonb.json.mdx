---
id: bach.SeriesJsonb.json
title: bach.SeriesJsonb.json — Bach  documentation
hide_title: true
slug: /modeling/series/bach.SeriesJsonb.json


---
<head>
    <meta name="description" content="Get access to json operations via the class that’s return through this accessor. Use as   or  class with accessor methods to json(b) type data columns. Slice this jsonb database object in pythonic ways:  – A very mixed key to slice on, please see below. Or select based on the objects   an array. With this method, a dict is passed in the   selector. The value of the first match with the dict to the objects in a json array is returned for the   selector. A match is when all key/value pairs of the " />
    <meta property="og:description" content="Get access to json operations via the class that’s return through this accessor. Use as   or  class with accessor methods to json(b) type data columns. Slice this jsonb database object in pythonic ways:  – A very mixed key to slice on, please see below. Or select based on the objects   an array. With this method, a dict is passed in the   selector. The value of the first match with the dict to the objects in a json array is returned for the   selector. A match is when all key/value pairs of the " />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/series/bach.SeriesJsonb.json.html')} />
