---
id: bach.DataFrame.drop
title: bach.DataFrame.drop — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.drop


---
<head>
    <meta name="description" content="Drop columns from the DataFrame  ( ) – not supported  ( ) – not supported  ( ) – the list of columns to drop.  ( ) – not supported  ( ) – update the current DataFrame or return a new DataFrame.  ( ) – ‘raise’ or ‘ignore’ missing key errors. DataFrame without the removed columns or None if  . Optional[ ] previous bach.DataFrame.cube next bach.DataFrame.expanding" />
    <meta property="og:description" content="Drop columns from the DataFrame  ( ) – not supported  ( ) – not supported  ( ) – the list of columns to drop.  ( ) – not supported  ( ) – update the current DataFrame or return a new DataFrame.  ( ) – ‘raise’ or ‘ignore’ missing key errors. DataFrame without the removed columns or None if  . Optional[ ] previous bach.DataFrame.cube next bach.DataFrame.expanding" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.drop.html')} />
