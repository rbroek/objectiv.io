---
id: bach.DataFrame.from_table
title: bach.DataFrame.from_table — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.from_table


---
<head>
    <meta name="description" content="Instantiate a new DataFrame based on the content of an existing table in the database. This will create and remove a temporary table to asses meta data for the setting the correct dtypes.  ( ) – an sqlalchemy engine for the database.  ( ) – the table name that contains the data to instantiate as DataFrame.  ( ) – list of column names that make up the index. At least one column needs to be selected for the index. A DataFrame based on a sql table. Note In order to create this temporary table the s" />
    <meta property="og:description" content="Instantiate a new DataFrame based on the content of an existing table in the database. This will create and remove a temporary table to asses meta data for the setting the correct dtypes.  ( ) – an sqlalchemy engine for the database.  ( ) – the table name that contains the data to instantiate as DataFrame.  ( ) – list of column names that make up the index. At least one column needs to be selected for the index. A DataFrame based on a sql table. Note In order to create this temporary table the s" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.from_table.html')} />
