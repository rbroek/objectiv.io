---
id: bach.DataFrame.get_sample
title: bach.DataFrame.get_sample — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.get_sample


---
<head>
    <meta name="description" content="Returns a DataFrame whose data is a sample of the current DataFrame object. For the sample Dataframe to be created, all data is queried once and a persistent table is created to store the sample data used for the sampled DataFrame. Use   to switch back to the unsampled data later on. This returns a new DataFrame with all operations that have been done on the sample, applied to that DataFrame.  ( ) – the name of the underlying sql table that stores the sampled data.  ( ) – a filter to apply to th" />
    <meta property="og:description" content="Returns a DataFrame whose data is a sample of the current DataFrame object. For the sample Dataframe to be created, all data is queried once and a persistent table is created to store the sample data used for the sampled DataFrame. Use   to switch back to the unsampled data later on. This returns a new DataFrame with all operations that have been done on the sample, applied to that DataFrame.  ( ) – the name of the underlying sql table that stores the sampled data.  ( ) – a filter to apply to th" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.get_sample.html')} />
