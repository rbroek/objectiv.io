---
id: bach.DataFrame.std
title: bach.DataFrame.std — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.std


---
<head>
    <meta name="description" content="Returns the sample standard deviation of each column.  – only   is supported. This means columns are aggregated.  – only   supported. This means NULL values are ignored.  – not supported.  ( ) – Delta Degrees of Freedom. Only 1 is supported.  – whether to aggregate numeric series only, or attempt all. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.sort_values next bach.DataFrame.sum" />
    <meta property="og:description" content="Returns the sample standard deviation of each column.  – only   is supported. This means columns are aggregated.  – only   supported. This means NULL values are ignored.  – not supported.  ( ) – Delta Degrees of Freedom. Only 1 is supported.  – whether to aggregate numeric series only, or attempt all. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.sort_values next bach.DataFrame.sum" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.std.html')} />
