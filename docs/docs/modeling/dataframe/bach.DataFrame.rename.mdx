---
id: bach.DataFrame.rename
title: bach.DataFrame.rename — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.rename


---
<head>
    <meta name="description" content="Rename columns. The interface is similar to Panda’s  . However we don’t support renaming indexes, so recommended usage is  .  ( ) – dict to apply to that axis’ values. Use mapper and axis to specify the axis to target with mapper. Currently mapper is only supported with  , which is similar to using columns.  ( ) – not supported.  ( ) – dict str:str to rename columns, or a function that takes column names as an argument and returns the new one. The new column names must not clash with other colum" />
    <meta property="og:description" content="Rename columns. The interface is similar to Panda’s  . However we don’t support renaming indexes, so recommended usage is  .  ( ) – dict to apply to that axis’ values. Use mapper and axis to specify the axis to target with mapper. Currently mapper is only supported with  , which is similar to using columns.  ( ) – not supported.  ( ) – dict str:str to rename columns, or a function that takes column names as an argument and returns the new one. The new column names must not clash with other colum" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.rename.html')} />
