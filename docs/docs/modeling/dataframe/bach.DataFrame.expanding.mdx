---
id: bach.DataFrame.expanding
title: bach.DataFrame.expanding — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.expanding


---
<head>
    <meta name="description" content="Create an expanding window starting with the first row in the group, with at least   observations. The result will be right-aligned in the window.  ( ) – the minimum amount of observations in the window before a value is reported.  ( ) – whether to center the result, currently not supported.  ( ) – the partition that will be applied. Note ‘partition’ is different from pandas, where the partition is determined earlier in the process. previous bach.DataFrame.drop next bach.DataFrame.from_model" />
    <meta property="og:description" content="Create an expanding window starting with the first row in the group, with at least   observations. The result will be right-aligned in the window.  ( ) – the minimum amount of observations in the window before a value is reported.  ( ) – whether to center the result, currently not supported.  ( ) – the partition that will be applied. Note ‘partition’ is different from pandas, where the partition is determined earlier in the process. previous bach.DataFrame.drop next bach.DataFrame.from_model" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.expanding.html')} />
