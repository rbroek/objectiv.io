---
id: bach.DataFrame.groupby
title: bach.DataFrame.groupby — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.groupby


---
<head>
    <meta name="description" content="Group by any of the series currently in this DataDrame, both from index as well as data.  ( ) –  The series to group by. Supported are: a string containing a columnn name. a series. a list of strings or series. A normal group by will be created. a list of (strings, series, lists). In this case a grouping list is created. a tuple of (strings, series, lists). In this case a grouping set is created. a new DataFrame object with the   attribute set. Note If the dataframe is already grouped, we’ll cre" />
    <meta property="og:description" content="Group by any of the series currently in this DataDrame, both from index as well as data.  ( ) –  The series to group by. Supported are: a string containing a columnn name. a series. a list of strings or series. A normal group by will be created. a list of (strings, series, lists). In this case a grouping list is created. a tuple of (strings, series, lists). In this case a grouping set is created. a new DataFrame object with the   attribute set. Note If the dataframe is already grouped, we’ll cre" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.groupby.html')} />
