---
id: bach.DataFrame.head
title: bach.DataFrame.head — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.head


---
<head>
    <meta name="description" content="Similar to   but only returns the first   rows.  ( ) – number of rows to query from database. a pandas DataFrame. pandas.core.frame.DataFrame Note This function queries the database. previous bach.DataFrame.groupby next bach.DataFrame.materialize" />
    <meta property="og:description" content="Similar to   but only returns the first   rows.  ( ) – number of rows to query from database. a pandas DataFrame. pandas.core.frame.DataFrame Note This function queries the database. previous bach.DataFrame.groupby next bach.DataFrame.materialize" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.head.html')} />
