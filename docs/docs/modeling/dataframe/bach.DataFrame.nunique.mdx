---
id: bach.DataFrame.nunique
title: bach.DataFrame.nunique — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.nunique


---
<head>
    <meta name="description" content="Returns the number of unique values in each column.  – only   is supported. This means columns are aggregated.  – only   supported. This means NULL values are ignored. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.mode next bach.DataFrame.rename" />
    <meta property="og:description" content="Returns the number of unique values in each column.  – only   is supported. This means columns are aggregated.  – only   supported. This means NULL values are ignored. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.mode next bach.DataFrame.rename" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.nunique.html')} />
