---
id: bach.DataFrame.reset_index
title: bach.DataFrame.reset_index — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.reset_index


---
<head>
    <meta name="description" content="Drops the current index. With reset_index, all indexes are removed from the DataFrame, so that the DataFrame does not have any index Series. A new index can be set with  .  ( ) – if True, the dropped index is added to the data columns of the DataFrame. If False it is removed.  ( ) – update the current DataFrame or return a new DataFrame. DataFrame with the index dropped or None if  . Optional[ ] previous bach.DataFrame.rename next bach.DataFrame.rolling" />
    <meta property="og:description" content="Drops the current index. With reset_index, all indexes are removed from the DataFrame, so that the DataFrame does not have any index Series. A new index can be set with  .  ( ) – if True, the dropped index is added to the data columns of the DataFrame. If False it is removed.  ( ) – update the current DataFrame or return a new DataFrame. DataFrame with the index dropped or None if  . Optional[ ] previous bach.DataFrame.rename next bach.DataFrame.rolling" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.reset_index.html')} />
