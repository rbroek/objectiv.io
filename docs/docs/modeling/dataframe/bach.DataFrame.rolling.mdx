---
id: bach.DataFrame.rolling
title: bach.DataFrame.rolling — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.rolling


---
<head>
    <meta name="description" content="A rolling window of size ‘window’, by default right aligned.  ( ) – the window size.  ( ) – the min amount of rows included in the window before an actual value is returned.  ( ) – center the result, or align the result on the right.  ( ) – the partition to use, see  .  ( ) – make the interval closed on the ‘right’, ‘left’, ‘both’ or ‘neither’ endpoints. Defaults to ‘right’, and the rest is currently unsupported. a new DataFrame object with the   attribute set with a  . Note The  ,   and   param" />
    <meta property="og:description" content="A rolling window of size ‘window’, by default right aligned.  ( ) – the window size.  ( ) – the min amount of rows included in the window before an actual value is returned.  ( ) – center the result, or align the result on the right.  ( ) – the partition to use, see  .  ( ) – make the interval closed on the ‘right’, ‘left’, ‘both’ or ‘neither’ endpoints. Defaults to ‘right’, and the rest is currently unsupported. a new DataFrame object with the   attribute set with a  . Note The  ,   and   param" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.rolling.html')} />
