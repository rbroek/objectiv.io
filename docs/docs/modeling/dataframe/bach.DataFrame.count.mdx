---
id: bach.DataFrame.count
title: bach.DataFrame.count — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.count


---
<head>
    <meta name="description" content="Count all non-NULL values in each column.  – only   is supported. This means columns are aggregated.  – not supported.  – whether to aggregate numeric series only, or attempt all. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.copy next bach.DataFrame.cube" />
    <meta property="og:description" content="Count all non-NULL values in each column.  – only   is supported. This means columns are aggregated.  – not supported.  – whether to aggregate numeric series only, or attempt all. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.copy next bach.DataFrame.cube" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.count.html')} />
