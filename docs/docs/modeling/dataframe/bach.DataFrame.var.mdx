---
id: bach.DataFrame.var
title: bach.DataFrame.var — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.var


---
<head>
    <meta name="description" content="Returns the unbiased variance of each column.  – only   is supported. This means columns are aggregated.  – only   supported. This means NULL values are ignored.  – not supported.  ( ) – Delta Degrees of Freedom. Only 1 is supported.  – whether to aggregate numeric series only, or attempt all. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.to_pandas next bach.DataFrame.view_sql" />
    <meta property="og:description" content="Returns the unbiased variance of each column.  – only   is supported. This means columns are aggregated.  – only   supported. This means NULL values are ignored.  – not supported.  ( ) – Delta Degrees of Freedom. Only 1 is supported.  – whether to aggregate numeric series only, or attempt all. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.to_pandas next bach.DataFrame.view_sql" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.var.html')} />
