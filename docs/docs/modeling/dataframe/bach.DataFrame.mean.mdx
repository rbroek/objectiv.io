---
id: bach.DataFrame.mean
title: bach.DataFrame.mean — Bach  documentation
hide_title: true
slug: /modeling/dataframe/bach.DataFrame.mean


---
<head>
    <meta name="description" content="Returns the mean of all values in each column.  – only   is supported. This means columns are aggregated.  – only   supported. This means NULL values are ignored.  – not supported.  – whether to aggregate numeric series only, or attempt all. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.max next bach.DataFrame.median" />
    <meta property="og:description" content="Returns the mean of all values in each column.  – only   is supported. This means columns are aggregated.  – only   supported. This means NULL values are ignored.  – not supported.  – whether to aggregate numeric series only, or attempt all. a new DataFrame with the aggregation applied to all selected columns. previous bach.DataFrame.max next bach.DataFrame.median" />
</head>

export const toc = [];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/dataframe/bach.DataFrame.mean.html')} />
