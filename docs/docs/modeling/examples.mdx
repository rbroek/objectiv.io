---
id: examples
title: Examples — Bach  documentation
hide_title: true
slug: /modeling/examples


---
<head>
    <meta name="description" content="Here we’ll give some very basic examples of the usage of Bach: creating a DataFrame, basic operations, aggregate operations, and getting the resulting data. In the examples we’ll assume that the database has a table called ‘example’, with a few specific columns. The SQL to create that table can be found below in  . We also have   that you can use to get a feel for Bach. The above fragment queries the database to get the table structure of the ‘example’ table. But it does not query any of the dat" />
    <meta property="og:description" content="Here we’ll give some very basic examples of the usage of Bach: creating a DataFrame, basic operations, aggregate operations, and getting the resulting data. In the examples we’ll assume that the database has a table called ‘example’, with a few specific columns. The SQL to create that table can be found below in  . We also have   that you can use to get a feel for Bach. The above fragment queries the database to get the table structure of the ‘example’ table. But it does not query any of the dat" />
</head>

export const toc = [
    {
        "value": "Create a DataFrame from a database table",
        "id": "create-a-dataframe-from-a-database-table",
        "children": [],
        "level": 2
    },
    {
        "value": "Basic operations",
        "id": "basic-operations",
        "children": [],
        "level": 2
    },
    {
        "value": "Aggregate operations",
        "id": "aggregate-operations",
        "children": [],
        "level": 2
    },
    {
        "value": "Filtering, sorting, and output",
        "id": "filtering-sorting-and-output",
        "children": [],
        "level": 2
    },
    {
        "value": "Appendix: Example Data",
        "id": "appendix-example-data",
        "children": [],
        "level": 2
    }
];


import SphinxPages from '@site/src/components/sphinx-page'
import useBaseUrl from '@docusaurus/useBaseUrl'


<SphinxPages url={useBaseUrl('modeling/examples.html')} />
